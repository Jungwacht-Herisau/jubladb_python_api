# This file is auto-generated by generate_entities.py
import jubladb_api.metamodel
import jubladb_api.entities.base


# @formatter:off
class {{ entity.name_singular | snake_case_to_pascal_case }}(jubladb_api.entities.base.EntityBase):
    def __init__(self,
                 id_: int,
                 {% for attr in entity.attributes %}
                 {{ attr.name }}: {{ attr.type_ | attribute_type_to_python_type }}, {% endfor %}
                 ):
        super().__init__(id_)
        {% for attr in entity.attributes %}
        self._{{ attr.name }} = {{ attr.name }} {% endfor %}

    {% for attr in entity.attributes %}
    @property
    def {{ attr.name }}(self) -> {{ attr.type_ | attribute_type_to_python_type }}:
        return self._{{ attr.name }}
    {% endfor %}

    @property
    def meta(self) -> jubladb_api.metamodel.Entity:
        return jubladb_api.metamodel.ENTITIES["{{ entity.name_singular }}"]

# @formatter:on